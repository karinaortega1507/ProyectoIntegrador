import { IonContent, IonText } from '@ionic/react'
import { S_IFCHR } from 'constants';
import React from 'react'

const UserContext : React.FC = () => {
  return (
    <IonContent>
      <IonText>UserContext</IonText>
    </IonContent>
  )
}

export default UserContext;
